# BalancePet 1.0.0-dev.33

- 修复主程序重启后，旧功能扩展进程未被接管，导致更新消息中心时 `BalancePet.NotificationCenter.dll` 被占用的问题。
- 桌宠点击区域调整为：上方 `2/3` 触发彩蛋，下方 `1/3` 静默刷新余额。
- 配套消息中心升级到 `0.5.13`：桌宠镜像到左侧后，鼠标悬停并按住 `Shift` 也可正常展示环绕信息。
- 用量统计扩展保持 `0.3.1`。
