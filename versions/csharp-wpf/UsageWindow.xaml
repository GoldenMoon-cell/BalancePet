<Window x:Class="BalancePet.Wpf.UsageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="用量统计" Width="430" Height="430"
        WindowStartupLocation="CenterOwner" ResizeMode="CanMinimize"
        Background="Transparent">
  <Window.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Themes/FluentControls.xaml"/>
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Window.Resources>
  <Grid Background="{DynamicResource WindowBackgroundBrush}">
  <Grid Margin="18">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <StackPanel Margin="0,0,0,12">
      <TextBlock Text="最近用量" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}"/>
      <TextBlock Text="本机保存的余额变化记录" Margin="0,4,0,0" Foreground="{DynamicResource MutedBrush}"/>
    </StackPanel>
    <ListView x:Name="HistoryList" Grid.Row="1" Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource TextBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="6">
      <ListView.View>
        <GridView AllowsColumnReorder="False">
          <GridViewColumn Header="日期" DisplayMemberBinding="{Binding Date}" Width="120"/>
          <GridViewColumn Header="货币" DisplayMemberBinding="{Binding Currency}" Width="76"/>
          <GridViewColumn Header="消耗" DisplayMemberBinding="{Binding UsageText}" Width="150"/>
        </GridView>
      </ListView.View>
    </ListView>
    <TextBlock x:Name="SummaryText" Grid.Row="2" Margin="0,12,0,0" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Right"/>
  </Grid>
  </Grid>
</Window>
