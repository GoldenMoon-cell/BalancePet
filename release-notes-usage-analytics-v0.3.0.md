# Usage Analytics v0.3.0

## 新增

- 新增“总余额”卡片，读取主程序 Balance Usage v1 的脱敏当前余额快照。
- 同币种账户会显示合计余额；不同币种不会进行无意义的跨币种相加。

## 修复与改进

- 保留“今日消费”的余额下降估算口径，并明确总余额来自最近一次成功余额查询。
- 余额快照不包含 Token、提示词、回复或 Cookie。

## 规范与兼容

- 插件版本：`v0.3.0`；接口版本仍为 `api_version: 1`。
- 需要 BalancePet 核心版本 `v0.9.0` 或更高版本；旧核心仍可启动插件，但不会提供总余额数据。

## 注意

- 总余额来自 BalancePet 已配置账户的最近成功查询，不是中转站账单，也不会推断充值或真实消费。

## 文件校验

- `balancepet.ext.feature.usage-analytics-0.3.0-win-x64.zip` SHA-256：`702f5c27a738d5682e6d4331c41be607959b8d5afb0027789b50794f89641f70`
