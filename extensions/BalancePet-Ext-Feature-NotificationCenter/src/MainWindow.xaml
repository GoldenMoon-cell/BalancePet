<Window x:Class="BalancePet.NotificationCenter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="消息中心" Icon="assets/balance-pet.ico" Width="820" Height="600" MinWidth="680" MinHeight="480"
        WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="CanResize"
        Background="{DynamicResource WindowBrush}" Foreground="{DynamicResource TextBrush}"
        FontFamily="Segoe UI, Microsoft YaHei UI" UseLayoutRounding="True">
  <shell:WindowChrome.WindowChrome>
    <shell:WindowChrome CaptionHeight="48" ResizeBorderThickness="6" CornerRadius="{DynamicResource WindowCornerRadius}" GlassFrameThickness="0" />
  </shell:WindowChrome.WindowChrome>
  <Window.Resources>
    <Style x:Key="IconButtonStyle" TargetType="Button">
      <Setter Property="Width" Value="46" /><Setter Property="Height" Value="48" />
      <Setter Property="Background" Value="Transparent" /><Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
      <Setter Property="BorderThickness" Value="0" /><Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button">
        <Border x:Name="Root" Background="{TemplateBinding Background}"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" /></Border>
        <ControlTemplate.Triggers>
          <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Root" Property="Background" Value="{DynamicResource ControlHoverBrush}" /></Trigger>
          <Trigger Property="IsPressed" Value="True"><Setter TargetName="Root" Property="Opacity" Value="0.72" /></Trigger>
        </ControlTemplate.Triggers>
      </ControlTemplate></Setter.Value></Setter>
    </Style>
    <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource IconButtonStyle}">
      <Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#C42B1C" /><Setter Property="Foreground" Value="White" /></Trigger></Style.Triggers>
    </Style>
    <Style x:Key="ActionButtonStyle" TargetType="Button">
      <Setter Property="Height" Value="36" /><Setter Property="Padding" Value="14,0" />
      <Setter Property="Background" Value="{DynamicResource PanelBrush}" /><Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
      <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" /><Setter Property="BorderThickness" Value="1" /><Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button">
        <Border x:Name="Root" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{DynamicResource ControlCornerRadius}"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" /></Border>
        <ControlTemplate.Triggers>
          <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Root" Property="Background" Value="{DynamicResource ControlHoverBrush}" /></Trigger>
          <Trigger Property="IsPressed" Value="True"><Setter TargetName="Root" Property="Opacity" Value="0.72" /></Trigger>
          <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource AccentBrush}" /></Trigger>
        </ControlTemplate.Triggers>
      </ControlTemplate></Setter.Value></Setter>
    </Style>
    <Style x:Key="FilterStyle" TargetType="RadioButton">
      <Setter Property="Height" Value="34" /><Setter Property="MinWidth" Value="64" /><Setter Property="Padding" Value="14,0" /><Setter Property="Margin" Value="0,0,8,0" />
      <Setter Property="Foreground" Value="{DynamicResource MutedBrush}" /><Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" /><Setter Property="BorderThickness" Value="1" /><Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="RadioButton">
        <Border x:Name="Root" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="17"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" /></Border>
        <ControlTemplate.Triggers>
          <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Root" Property="Background" Value="{DynamicResource ControlHoverBrush}" /></Trigger>
          <Trigger Property="IsChecked" Value="True"><Setter TargetName="Root" Property="Background" Value="{DynamicResource AccentSoftBrush}" /><Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource AccentBrush}" /><Setter Property="Foreground" Value="{DynamicResource AccentBrush}" /><Setter Property="FontWeight" Value="SemiBold" /></Trigger>
          <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource AccentBrush}" /></Trigger>
        </ControlTemplate.Triggers>
      </ControlTemplate></Setter.Value></Setter>
    </Style>
    <Style TargetType="ListBoxItem">
      <Setter Property="Padding" Value="0" /><Setter Property="Margin" Value="0,0,0,8" /><Setter Property="HorizontalContentAlignment" Value="Stretch" />
      <Setter Property="Background" Value="Transparent" /><Setter Property="BorderThickness" Value="0" /><Setter Property="FocusVisualStyle" Value="{x:Null}" />
    </Style>
  </Window.Resources>

  <Grid>
    <Grid.RowDefinitions><RowDefinition Height="48" /><RowDefinition Height="*" /></Grid.RowDefinitions>
    <Border Grid.Row="0" Background="{DynamicResource TitleBarBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
      <Grid>
        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
          <Border Width="28" Height="28" CornerRadius="8" Background="{DynamicResource AccentBrush}" Margin="0,0,10,0">
            <Path Data="M5,3 L15,3 C16.1,3 17,3.9 17,5 L17,13 C17,14.1 16.1,15 15,15 L10.5,15 L7,18 L7,15 L5,15 C3.9,15 3,14.1 3,13 L3,5 C3,3.9 3.9,3 5,3 Z M6,7 L14,7 M6,10 L12,10" Stroke="White" StrokeThickness="1.4" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Stretch="Uniform" Width="18" Height="18" />
          </Border>
          <TextBlock Text="BalancePet" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" />
          <TextBlock Text="消息中心" Foreground="{DynamicResource MutedBrush}" Margin="12,0,0,0" VerticalAlignment="Center" />
        </StackPanel>
        <Button Grid.Column="1" Style="{StaticResource IconButtonStyle}" shell:WindowChrome.IsHitTestVisibleInChrome="True" Click="MinimizeClick" ToolTip="最小化"><Path Data="M2,8 L14,8" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.2" Width="12" Height="12" Stretch="Uniform" /></Button>
        <Button Grid.Column="2" Style="{StaticResource IconButtonStyle}" shell:WindowChrome.IsHitTestVisibleInChrome="True" Click="MaximizeClick" ToolTip="最大化"><Path Data="M3,3 L13,3 L13,13 L3,13 Z" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.2" Fill="Transparent" Width="11" Height="11" Stretch="Uniform" /></Button>
        <Button Grid.Column="3" Style="{StaticResource CloseButtonStyle}" shell:WindowChrome.IsHitTestVisibleInChrome="True" Click="CloseClick" ToolTip="关闭"><Path Data="M3,3 L13,13 M13,3 L3,13" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.25" Width="11" Height="11" Stretch="Uniform" /></Button>
      </Grid>
    </Border>

    <Grid Grid.Row="1" Margin="24,20,24,18">
      <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
      <Grid Grid.Row="0" Margin="0,0,0,18">
        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
        <StackPanel><TextBlock Text="消息中心" FontSize="26" FontWeight="SemiBold" /><TextBlock Text="集中查看桌宠最近显示过的状态与提醒。" Foreground="{DynamicResource MutedBrush}" FontSize="13" Margin="0,4,0,0" /></StackPanel>
      </Grid>

      <Border Grid.Row="1" Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="{DynamicResource PanelCornerRadius}" Padding="16,13" Margin="0,0,0,14">
        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
          <Border Width="34" Height="34" CornerRadius="9" Background="{DynamicResource AccentSoftBrush}" Margin="0,0,12,0"><Path Data="M4,5 L16,5 C17.1,5 18,5.9 18,7 L18,15 C18,16.1 17.1,17 16,17 L10.5,17 L7,20 L7,17 L4,17 C2.9,17 2,16.1 2,15 L2,7 C2,5.9 2.9,5 4,5 Z M6,9 L14,9 M6,12 L12,12" Stroke="{DynamicResource AccentBrush}" StrokeThickness="1.4" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Stretch="Uniform" Width="20" Height="20" /></Border>
          <StackPanel Grid.Column="1" VerticalAlignment="Center"><TextBlock x:Name="SummaryText" Text="暂无消息" FontWeight="SemiBold" FontSize="14" /><TextBlock Text="最多展示最近 500 条记录" Foreground="{DynamicResource MutedBrush}" FontSize="11" Margin="0,2,0,0" /></StackPanel>
          <Border Grid.Column="2" Background="{DynamicResource AccentSoftBrush}" CornerRadius="12" Padding="10,5" VerticalAlignment="Center"><TextBlock Text="本地只读 · 不含令牌" Foreground="{DynamicResource AccentBrush}" FontSize="11" FontWeight="SemiBold" /></Border>
        </Grid>
      </Border>

      <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Margin="0,0,0,14">
        <StackPanel Orientation="Horizontal">
          <RadioButton Content="全部" Tag="all" GroupName="Filters" IsChecked="True" Style="{StaticResource FilterStyle}" Checked="FilterChecked" />
          <RadioButton Content="余额" Tag="balance" GroupName="Filters" Style="{StaticResource FilterStyle}" Checked="FilterChecked" />
          <RadioButton Content="刷新" Tag="refresh" GroupName="Filters" Style="{StaticResource FilterStyle}" Checked="FilterChecked" />
          <RadioButton Content="任务" Tag="task" GroupName="Filters" Style="{StaticResource FilterStyle}" Checked="FilterChecked" />
          <RadioButton Content="账户" Tag="account" GroupName="Filters" Style="{StaticResource FilterStyle}" Checked="FilterChecked" />
          <RadioButton Content="系统" Tag="system" GroupName="Filters" Style="{StaticResource FilterStyle}" Checked="FilterChecked" />
          <RadioButton Content="互动" Tag="interaction" GroupName="Filters" Style="{StaticResource FilterStyle}" Checked="FilterChecked" />
        </StackPanel>
      </ScrollViewer>

      <Border Grid.Row="3" Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="{DynamicResource PanelCornerRadius}" ClipToBounds="True">
        <Grid>
          <ListBox x:Name="EventsList" Background="Transparent" BorderThickness="0" Padding="10" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate><DataTemplate><Border Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="{DynamicResource CardCornerRadius}" Padding="14,12">
              <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="4" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <Border Width="4" Background="{Binding CategoryBrush}" CornerRadius="2" Margin="0,1,12,1" />
                <StackPanel Grid.Column="1"><DockPanel LastChildFill="True"><TextBlock DockPanel.Dock="Right" Text="{Binding OccurredAtText}" Foreground="{DynamicResource MutedBrush}" FontSize="11" Margin="16,2,0,0" /><TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" /></DockPanel><TextBlock Text="{Binding Amount}" Foreground="{DynamicResource AccentBrush}" FontWeight="SemiBold" Margin="0,5,0,0" Visibility="{Binding AmountVisibility}" /><TextBlock Text="{Binding Detail}" Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap" Margin="0,3,0,0" /></StackPanel>
                <Border Grid.Column="2" Background="{DynamicResource WindowBrush}" CornerRadius="11" Padding="9,4" Margin="14,0,0,0" VerticalAlignment="Top"><TextBlock Text="{Binding CategoryText}" Foreground="{Binding CategoryBrush}" FontSize="11" FontWeight="SemiBold" /></Border>
              </Grid>
            </Border></DataTemplate></ListBox.ItemTemplate>
          </ListBox>
          <StackPanel x:Name="EmptyState" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="360" Visibility="Collapsed">
            <Border Width="64" Height="64" CornerRadius="20" Background="{DynamicResource AccentSoftBrush}" HorizontalAlignment="Center"><Path Data="M6,5 L18,5 C19.1,5 20,5.9 20,7 L20,15 C20,16.1 19.1,17 18,17 L12,17 L8,21 L8,17 L6,17 C4.9,17 4,16.1 4,15 L4,7 C4,5.9 4.9,5 6,5 Z M8,9 L16,9 M8,12 L14,12" Stroke="{DynamicResource AccentBrush}" StrokeThickness="1.5" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Stretch="Uniform" Width="30" Height="30" /></Border>
            <TextBlock Text="这里还很安静" FontSize="17" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,16,0,0" />
            <TextBlock x:Name="EmptyText" Text="下一次桌宠显示气泡时，消息摘要会自动出现在这里。" Foreground="{DynamicResource MutedBrush}" FontSize="12" TextAlignment="Center" TextWrapping="Wrap" Margin="0,6,0,0" />
          </StackPanel>
        </Grid>
      </Border>

      <DockPanel Grid.Row="4" Margin="2,12,2,0"><TextBlock x:Name="StatusText" Text="本地保存 · 自动随桌宠更新" Foreground="{DynamicResource MutedBrush}" FontSize="11" VerticalAlignment="Center" /><TextBlock DockPanel.Dock="Right" Text="消息中心 · v0.5.15" Foreground="{DynamicResource MutedBrush}" FontSize="11" /></DockPanel>
    </Grid>
  </Grid>
</Window>
