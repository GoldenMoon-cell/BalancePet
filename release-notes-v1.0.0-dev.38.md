# BalancePet v1.0.0-dev.38

## 修复

- 更新检查、更新下载和扩展更新提示恢复使用桌宠原生气泡，不再被消息中心环绕展示层静默拦截。
- 保留“发现扩展更新”的消息中心事件记录，同时确保手动检查更新时用户能直接看到结果。

## 验证

- 主程序 Release 构建通过。
- 发布包包含便携 ZIP 和 Setup.exe；SHA-256 记录在 `WORK_STATUS.md`。
