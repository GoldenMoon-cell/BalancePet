# BalancePet v1.1.0

相比上一正式版 `v1.0.0`，`v1.1.0` 继续完善用量统计、扩展管理和桌面端更新流程。

## 新增

- **逐次请求用量明细**：读取 Codex 本地逐次 Token 记录，在一条任务记录下保留每次请求的输入、输出、缓存、模型和思考强度。
- **中转站明细逐条匹配**：将本地请求明细与 New API 只读日志逐条关联，再汇总任务额度，减少整段任务匹配失败导致的“未上报”。
- **中转站明细缓存**：缓存最近中转站请求明细，网络暂时不可用时可以使用可重建缓存；缓存不包含 API Key，并可独立删除。
- **扩展库清理**：清理旧版时同时处理旧版本 ZIP、同版本重复 ZIP 和已解压的旧运行副本。

## 改进

- Codex 用量读取只扫描当前会话最近的 Rollout 文件，并避免短时间内重复扫描，降低任务完成后的磁盘开销。
- Usage Analytics 保留任务级汇总，同时支持展开查看逐次请求明细；明细行容量和插件读取上限同步提高。
- 重复导入同一个扩展版本时覆盖标准 ZIP，不再不断生成带 GUID 的长文件名副本。
- 保留旧任务的聚合匹配逻辑，兼容升级前已有的用量记录。
- 扩展 ZIP 和主程序资源继续使用 SHA-256 校验，未改变令牌 DPAPI 保护和主进程凭证边界。

## 扩展版本

- Usage Analytics `v0.3.9`
- Notification Center `v0.5.15`
- BalancePet Mica `v1.0.0`

## 文件校验

- `BalancePet-1.1.0-win-x64.zip` SHA-256：`7d5210741f56490223f76a5c8d0bc874ffbdf6bf7564c0ec89dd26b32a5432b1`
- `BalancePet-1.1.0-Setup.exe` SHA-256：`a4fe42661d13f1fc58362fa2d2502945eca86d4c2b0085b423a77933984f5476`
- `balancepet.ext.feature.usage-analytics-0.3.9-win-x64.zip` SHA-256：`4a97cee0a58f9d90cc9a348f9579e86ec110dc423fe69850d4a9a6327d82ae5c`
