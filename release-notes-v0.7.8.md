# BalancePet v0.7.8

## 新增

- 主程序接入 Usage Event v1 用量管道，支持脱敏 Token、缓存计数、请求耗时和可选首 Token 时间。
- 功能扩展宿主支持独立进程、能力声明、独立 GitHub Release 更新检查和安全 ZIP 校验。
- 发布功能扩展接口规范 v1，明确 manifest、启动参数、事件格式、生命周期和安全边界。

## 修复与改进

- 修复 Codex 停止钩子缺少会话身份时无法关联本地 Token 记录的问题。
- 未上报的可选用量字段直接省略，不再伪装成 0 或 null。
- 加强扩展清单、入口路径、压缩包路径、文件类型和 GitHub 更新地址校验。
- 更新设置窗口、统计窗口、自动刷新倒计时和用量展示。

## 规范与兼容

- 主程序当前版本：`v0.7.8`。
- 功能扩展接口继续使用 `api_version: 1`；插件自己的语义版本独立递增。
- 第三方功能扩展只需遵循接口、生命周期、数据和安全规范，不强制统一 UI、主题、Logo、字体或窗口布局。
- Python fallback 保留，扩展以独立进程运行，不加载到主程序进程。

## 注意

- 主程序只接收脱敏计数和耗时等元数据，不接收提示词、回复、Cookie、API Key 或其他凭据。
- 功能扩展会以当前用户权限运行，请只安装可信来源的扩展包。
- 若客户端没有上报 Token、缓存或首 Token 数据，对应指标会显示为未上报。

## 文件校验

- `BalancePet-0.7.8-win-x64.zip` SHA-256：`ff850f8423d0ef6305ef1cb9bb0d43fd2b5b8e4b40cd7506fd74001398cb942e`
- `BalancePet-0.7.8-Setup.exe` SHA-256：`8f4c307047a7b0e898a97a360691f7aac89bb8c0d21c0a3d7b184b78b40a863d`
