# BalancePet 1.0.0-dev.31

- 恢复桌宠点击彩蛋：点击会先展示当前形象的互动反馈，再在后台静默刷新余额。
- 用量统计插件可通过本地无凭据信号唤醒主程序的到期自动刷新调度。
- 功能插件卸载后，对应右键菜单和托盘入口会消失；仅禁用时显示“插件未启用”。
- 设置面板支持拖入扩展 ZIP 直接安装，并新增旧版扩展资源清理。
- Usage Event v1 增加可选的 `cost` 与 `currency` 脱敏字段。
- 配套插件：用量统计 `0.3.1`、消息中心 `0.5.12`。
